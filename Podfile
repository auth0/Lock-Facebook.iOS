# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'LockFacebook' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LockFacebook
  pod 'Auth0', '~> 1.2'
  pod 'FacebookLogin'

  target 'LockFacebookTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'LockFacebookApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LockFacebookApp

end
